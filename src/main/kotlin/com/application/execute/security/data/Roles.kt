package com.application.execute.security.data
/**********************************************************************************************************************/
enum class Roles(val type: String) {
    ADMIN("ADMIN"),
    USER("USER")
}